<div id="wrapper">
    <a href="/scrape">start scraping...</a>
    <h1>Note Scraper</h1>
    <h2><i>If you can do this...the homework should be no problem.</i></h2>
    {{#each scrapes}}
        {{unless saved}}
            <div id="articles">
                {{title}}
            </div>
            <div id="notes">
                {{note}}
            </div>
        {{/unless}}
    {{/each}}
</div>